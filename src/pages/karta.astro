---
// /karta's explorer (tabs, filters, map, detail panel) was merged into
// /farmers — this route now only 301-redirects so old links/bookmarks/search
// results keep resolving. See src/pages/farmers.astro + src/scripts/farmer-map.ts.
export const prerender = false;

return Astro.redirect('/farmers', 301);
---
